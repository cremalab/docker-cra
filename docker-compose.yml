version: '3'

services:
  app:
    build: .
    container_name: app
    working_dir: /app
    command: bash -c "npm install && npm start"
    volumes:
      - ./app/:/app
      - /app/node_modules
    ports:
      - "3000:3000"
